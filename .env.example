# ─────────────────────────────────────────────────────────────
# JIT Sales Enablement Engine — Environment Variables
# ─────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values.
# The MCP server (KB management) works without any of these.
# The webhook server needs only a messaging platform token.
# ANTHROPIC_API_KEY is optional — without it, enablement packages
# use template-based formatting from your KB content. With it,
# Claude generates AI-refined prose.
# ─────────────────────────────────────────────────────────────

# Optional — enables AI-generated enablement prose (omit for template-based)
ANTHROPIC_API_KEY=

# Slack integration
SLACK_BOT_TOKEN=xoxb-...
PMM_SLACK_ID=

# Telegram integration (optional — use Slack OR Telegram)
TELEGRAM_BOT_TOKEN=
PMM_TELEGRAM_CHAT_ID=

# Webhook server config
WEBHOOK_PORT=3456
CHANNEL=slack

# Data file paths (defaults to ./data/ — usually no need to change)
# DATA_DIR=./data

# ── KB Sync (optional) ──────────────────────────────────────
# When set, local KB changes (via Claude Code) push automatically
# to your deployed webhook server. Generate a random secret and
# set the same value on both your local .env and Railway.
# SYNC_URL=https://your-app.up.railway.app
# SYNC_SECRET=your-random-secret-here
